# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (contains Supabase secrets — NEVER commit)
.env
.env.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bun lock file — excluded to avoid "multiple lock files" error on DigitalOcean.
# npm's package-lock.json is the primary lock file for deployment; bun.lockb
# is kept locally for developers who prefer Bun.
bun.lockb
